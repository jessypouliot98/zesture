{
  "name": "zesture",
  "module": "index.ts",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "build:dev": "bun build --entrypoints ./index.ts --outdir ./dist",
    "build:prod": "bun build --entrypoints ./index.ts --outdir ./dist --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}